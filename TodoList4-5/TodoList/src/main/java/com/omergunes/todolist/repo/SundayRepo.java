package com.omergunes.todolist.repo;

import com.omergunes.todolist.model.Sunday;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SundayRepo extends JpaRepository<Sunday, Long>  {
}
